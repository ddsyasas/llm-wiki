{
  "name": "@llm-wiki/core",
  "version": "0.0.0",
  "private": true,
  "description": "Three operations (ingest, query, lint) plus wiki and schema I/O.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'lint: no-op for now'",
    "build": "echo 'build: no-op for now'"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "vitest": "^2.1.9"
  }
}
